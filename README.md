# tdl-extensions
Telegram Downloader extensions


duplicate_sorter.py - file with script that loads result.json with TG channel or TG group messages, extract all duplicate files (same file name, different file size) and save them to duplicates.json. Also the script will check you download directory for existing files and save only unique files list not existing on your disk to download.json
